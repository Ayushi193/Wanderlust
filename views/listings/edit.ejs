<%  layout("/layouts/boilerplate") %>
    <h3>Edit your listing</h3>
    <form method="POST" action="/listings">
    <div class="mb-3">
        <lable for="title" class="form-lable">Title</lable>
        <input name="listing[title]" value="<%= listing.title %>" type="text" class="form-control">
    </div>
         <div class="mb-3">
            <lable for="description" class="form-lable">discription</lable>
        <textarea name="listing[description]"></textarea>value="<%= listing.description"%></textarea>
    </div>
    <div>
        <lable for="Image" class="form-lable">Image</lable>
        <input name="listing[image]"value="listing.image"  type="text"/>
    </div>
    <div>
        <lable for="price" class="form-lable">Price</lable>
        <input name="listing[price]"value="listing.price"  type="number"/>
    </div>
    <div>
        <lable for="country" class="form-lable">Country</lable>
        <input name="listing[country]"value="listing.country"  type="text"/>
        </div>
        <div>
            <lable for="location" class="form-lable">Location</lable>
        <input name="listing[location]"value="listing.location"  type="text"/>
        </div>
    <br></br>
        <button class="btn btn-dark edit-btn">Edit</button>
    </form>